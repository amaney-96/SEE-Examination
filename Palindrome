#include <stdio.h>
#include <string.h>
int main() {
    char s[20];
    int i, n;

    scanf("%s", s);
    n = strlen(s);

    for(i = 0; i < n/2; i++)
        if(s[i] != s[n-i-1]) {
            printf("Not Palindrome");
            return 0;
        }

    printf("Palindrome");
    return 0;
}
